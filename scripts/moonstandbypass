local Old_
Old_ = hookmetamethod(game, "__namecall", newcclosure(function(___, ...)
    local Args = {...}
    local Method = getnamecallmethod():lower()

    if Method == "httpget" or Method == "httpgetasync" then
        local url = Args[1]

        if url:find("raw%.githubusercontent%.com/Mander120/bypass%-prem/refs/heads/main/Bypass%%20acces") or url:find("raw%.githubusercontent%.com/Mander120/premium/refs/heads/main/premium%%20acces") then
            return [[
                return {
                    [1] = true,
                }
            ]]
        end
    end

    return Old_(___, ...)
end))

getgenv().Script = "Get Moon Stand for free at discord.gg/mfyCBWWExF"

getgenv().Owner = "debrainers"
getgenv().DisableRendering = false
getgenv().BlackScreen = false
getgenv().FPSCap = 60
getgenv().Guns = {"rifle", "aug", "flamethrower"} -- rifle, aug, flintlock, lmg, db, flamethrower

loadstring(game:HttpGet("https://raw.githubusercontent.com/Mander120/MoonStandV1/refs/heads/main/MoonStandV1"))()
